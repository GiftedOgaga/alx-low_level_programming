#include <stdio.h>

/**
 * _isupper - Check for uppercase
 * @c: print int
 * Return: 1 if c is uppercase otherwise 0
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		Return(1);
	}
	else
	{
		Return(0);
	}
}
