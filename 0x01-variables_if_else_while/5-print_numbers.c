#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int Sdigit = '1';

	for (Sdigit = 0; Sdigit < 10; Sdigit++)
	{
		printf("%d", Sdigit);
	}
		printf("\n");
		return (0);
}
